export const setLoginToken = (state, token) => {
  state.token = token;
};

export const setLoading = (state, isLoading) => {
  state.isLoading = isLoading;
};

export const setHeadList = (state,headList) => {
  state.headList = headList;
};
